<div class='main-wrapper'>
  <h2><%= @user.nick_name %>さんをフォローしてるユーザー一覧</h2>
  <div class='content-wrapper'>
    <% if @users.exists? %>
      <%= render 'users/index', users: @users %>
    <% else %>
      <p>ユーザーはいません。</p>
    <% end %>
  </div>
</div>